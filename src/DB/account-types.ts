import { AccountType } from "../models/models";

var accountTypes: AccountType[] = [
  new AccountType(1, "Bank"),
  new AccountType(2, "Credit Card"),
  new AccountType(3, "Loan"),
  new AccountType(4, "Subscription"),
];

export default accountTypes;
